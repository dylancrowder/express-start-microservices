{
  "name": "express-start-microservices",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "auth",
    "products",
    "gateway",
    "common"
  ],
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/express-serve-static-core": "^5.0.7",
    "turbo": "^2.5.5"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start:auth": "turbo run dev --filter=auth"
  },
  "packageManager": "npm@9.8.0"
}
